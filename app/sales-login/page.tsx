"use client";
import { LockKeyhole, LogIn, UserRound } from "lucide-react";
import { type FormEvent, useState } from "react";
export default function SalesLogin(){const[identifier,setIdentifier]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/sales-auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({identifier,password})});const data=await r.json();if(!r.ok)throw new Error(data.error);window.location.href="/crm/today"}catch(e){setError(e instanceof Error?e.message:"Login failed.")}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center bg-slate-100 p-4"><div className="grid w-full max-w-4xl overflow-hidden rounded-2xl bg-white shadow-2xl md:grid-cols-[.95fr_1.05fr]"><section className="bg-slate-950 p-8 text-white md:p-10"><img alt="Coach For Life" className="h-14 w-auto" src="/brand/coach-for-life-logo-horizontal.png"/><p className="mt-10 text-xs font-black uppercase tracking-[.18em] text-emerald-300">Sales Portal</p><h1 className="mt-3 text-4xl font-black leading-tight">Your priority leads. Your next action.</h1><p className="mt-4 text-sm font-semibold leading-6 text-slate-300">Secure counselor access to assigned leads, follow-ups, calls, meetings and session scorecards.</p></section><form className="p-8 md:p-10" onSubmit={submit}><span className="grid size-11 place-items-center rounded-xl bg-emerald-50 text-emerald-700"><LockKeyhole className="size-5"/></span><h2 className="mt-5 text-3xl font-black">Sales sign in</h2><p className="mt-2 text-sm font-semibold text-slate-500">Use the email/mobile number and password created by your admin.</p>{error?<p className="mt-4 rounded-xl bg-rose-50 px-4 py-3 text-sm font-bold text-rose-700">{error}</p>:null}<label className="mt-6 block text-sm font-black">Email or mobile number<div className="relative mt-2"><UserRound className="absolute left-3 top-3.5 size-4 text-slate-400"/><input autoComplete="username" className="w-full rounded-xl border border-slate-300 py-3 pl-10 pr-3 font-bold" onChange={(e)=>setIdentifier(e.target.value)} required value={identifier}/></div></label><label className="mt-4 block text-sm font-black">Password<input autoComplete="current-password" className="mt-2 w-full rounded-xl border border-slate-300 px-3 py-3 font-bold" onChange={(e)=>setPassword(e.target.value)} required type="password" value={password}/></label><button className="mt-6 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-emerald-600 px-4 py-3.5 text-sm font-black text-white disabled:opacity-50" disabled={busy} type="submit"><LogIn className="size-4"/>{busy?"Signing in…":"Login to Sales CRM"}</button><a className="mt-5 block text-center text-sm font-bold text-slate-500" href="/login">Master Admin Login</a></form></div></main>}
